# Required Configuration
# -----------------------

# Cloudflare API token with Zone.DNS permissions
# This can also be specified in config.yaml under cloudflare.api_token
# Keeping it in .env is more secure
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token

# Email address for certificate registration (can be moved to config.yaml)
CERTBOT_EMAIL=your_email@example.com

# Domains for certificates (semicolon-separated for multiple certificates)
# Can be specified in config.yaml instead
# Format: domain.com,www.domain.com;another.com,*.another.com
DOMAINS=example.com,www.example.com

# Optional Configuration
# -----------------------
# Note: All these settings can be specified in config.yaml instead

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Log file (absolute path, optional)
# LOG_FILE=/var/log/lecf.log

# Certificate renewal threshold in days (default: 30)
# CERT_RENEWAL_THRESHOLD_DAYS=30

# Certificate check interval in hours (default: 12)
# CERT_CHECK_INTERVAL_HOURS=12

# DDNS check interval in minutes (default: 15)
# DDNS_CHECK_INTERVAL_MINUTES=15

# Certificate storage directory
# CERT_DIR=/etc/letsencrypt/live 